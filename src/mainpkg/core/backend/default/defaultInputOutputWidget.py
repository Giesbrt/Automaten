from PySide6.QtWidgets import QFrame

# Standard typing imports for aps
import collections.abc as _a
import abc as _abc
import typing as _ty
import types as _ts


class DefaultInputOutputWidget(QFrame):
    def __init__(self, /, parent=..., f=...):
        super().__init__(parent, f, frameShape=None, frameShadow=None, lineWidth=None,
                         midLineWidth=None, frameWidth=None, frameRect=None)

        # TODO (p. 03) maybe add a default
